import React from "react";
import { BackgroundPhoto, BackgroundCover } from "./account.styles";

const LoginScreen = () => {
  // Use TextInput field from react native paper
  return (
    <BackgroundPhoto>
      <BackgroundCover />
    </BackgroundPhoto>
  );
};

export default LoginScreen;
